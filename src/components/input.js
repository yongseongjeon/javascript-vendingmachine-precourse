export const input = ({ className, placeholder, type }) => {
  return `<input 
            class="${className}" 
            placeholder="${placeholder}"
            type="${type}" 
          />`;
};
